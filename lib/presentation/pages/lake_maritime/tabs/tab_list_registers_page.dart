import 'package:flutter/material.dart';

class TabListRegistersPage extends StatelessWidget {
  const TabListRegistersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Listado de registros enviados"),
    );
  }
}
